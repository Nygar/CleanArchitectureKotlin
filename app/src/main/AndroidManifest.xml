<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
            android:networkSecurityConfig="@xml/network_security_config"
            android:enableOnBackInvokedCallback="true"
            android:name="cleancode.AndroidApplication"
            android:allowBackup="true"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name="cleancode.ui.activity.SplashActivity"
            android:noHistory="true"
            android:theme="@style/SplashStyle"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="cleancode.ui.activity.LoginActivity"/>

        <activity android:name="cleancode.ui.activity.MainActivity"/>

        <activity android:name="cleancode.ui.activity.MessageActivity"/>

        <activity android:name="cleancode.ui.activity.UserActivity"/>

    </application>
</manifest>
